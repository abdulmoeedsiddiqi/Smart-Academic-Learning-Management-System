<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- Custom CSS with Tailwind -->
  <link rel="stylesheet" href="/css/auth.output.css">
  <!-- Alpine.js for interactions - load before any Alpine components -->
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.13.0/dist/cdn.min.js"></script>
</head>
<body class="auth-body">
  <div class="auth-container">
    <!-- Floating shaped background elements for visual interest -->
    <div class="fixed top-0 left-0 w-full h-full pointer-events-none overflow-hidden z-0">
      <!-- Decorative elements -->
      <div class="absolute top-[15%] right-[10%] w-64 h-64 bg-blue-400 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob"></div>
      <div class="absolute top-[20%] left-[15%] w-72 h-72 bg-purple-400 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-2000"></div>
      <div class="absolute bottom-[15%] right-[20%] w-80 h-80 bg-yellow-400 rounded-full mix-blend-multiply filter blur-3xl opacity-20 animate-blob animation-delay-4000"></div>
    </div>
    
    <div class="auth-card relative z-10">
      <div class="auth-header bg-primary text-white">
        <div class="auth-logo-container">
          <img src="/images/salamslogo.png" alt="SALAMS Logo" class="auth-logo">
        </div>
        <h1 class="auth-title text-white">SALAMS</h1>
        <p class="auth-subtitle text-white">Smart Academic Learning and Management System</p>
      </div>
      <div class="auth-body">
        <!-- Flash Messages -->
        <%- include('../partials/messages') %>
        
        <!-- Content from child views -->
        <%- body %>
      </div>
    </div>
    
    <!-- Footer -->
    <div class="text-center mt-6 text-white text-sm">
      <p>&copy; <%= new Date().getFullYear() %> SALAMS. All rights reserved.</p>
    </div>
  </div>

  <!-- Custom Javascript -->
  <script src="/js/alpine-init.js"></script>
  <script src="/js/script.js"></script>
  
  <!-- Animation styles -->
  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob {
      animation: blob 7s infinite;
    }
    .animation-delay-2000 {
      animation-delay: 2s;
    }
    .animation-delay-4000 {
      animation-delay: 4s;
    }
    /* Apply Poppins font to all elements */
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</body>
</html>
